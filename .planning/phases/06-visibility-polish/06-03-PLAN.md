---
phase: 06-visibility-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - src/components/workflow/collapsible-log-section.tsx
  - src/components/workflow/log-entry.tsx
  - src/lib/db/queries.ts
  - src/app/api/task-logs/route.ts
  - tests/components/collapsible-log-section.test.tsx
          - tests/lib/db/queries-logs.test.ts
autonomous: true
requirements:
  - VIS-04
user_setup: []
must_haves:
  truths:
    - "User can expand task row to view execution logs"
            - "Logs show timestamp, level (info/success/error/warning), and message"
            - "Logs persisted to agent_messages table"
            - "Historical workflow logs can be viewed from message history"
            - "Log section supports auto-expand for failed tasks"
            - "Process information displays compactly in the panel"
  artifacts:
    - path: "src/components/workflow/collapsible-log-section.tsx"
      provides: "Expandable log viewer component"
      min_lines: 60
      exports: ["CollapsibleLogSection", "CollapsibleLogSectionProps"]
    - path: "src/components/workflow/log-entry.tsx"
      provides: "Single log entry with timestamp and level styling"
      min_lines: 35
      exports: ["LogEntry"]
    - path: "src/lib/db/queries.ts"
      provides: "Log query functions"
      contains: "getTaskLogs"
    - path: "tests/lib/db/queries-logs.test.ts"
      provides: "Unit tests for log query functions"
      min_lines: 40
      exports: ["getTaskLogs"]
    - path: "src/app/api/task-logs/route.ts"
      provides: "API endpoint for fetching task logs"
      exports: ["GET"]
    - path: "tests/lib/db/queries-logs.test.ts"
      provides: "Unit tests for log query functions"
      min_lines: 40
      exports: ["getTaskLogs"]
  key_links:
    - from: "CollapsibleLogSection"
      to: "/api/task-logs"
      via: "fetch"
      pattern: "fetch.*task-logs"
    - from: "/api/task-logs"
      to: "getTaskLogs"
      via: "drizzle query"
        pattern: "prisma\\.(find|create)\\(orderBy(agentMessages.createdAt)"
  </action>
  <verify>
    <automated>MISSING — Wave 0 tests must for this test file pattern.
    Use queries.ts pattern for getTaskLogs. getMessagesByTask from './queries';

    <behavior>
  <task type="auto" tdd="true">
    <name>Task 1: Write interface contracts</name>
    <files>
      tests/lib/db/queries.ts
      tests/components/agent-status-list.test.tsx
      tests/components/collapsible-log-section.test.tsx
      tests/lib/db/queries-logs.test.ts
    </files>
  </read_first>
  <behavior>
    - Test 1: LogEntry renders timestamp in HH:MM:SS format
               - Render with level="info" and expect text-muted-foreground class
               - Render with level="success" and expect text-green-600 class
               - Render with level="error" and expect text-destructive class
               - Render with level="warning" and expect text-amber-600 class
              </behavior>
              - Test 5: LogEntry renders with XCircle icon for error level
                - Render with level="error" and expect text-destructive class and `{XCircle}` to  "Error message" icon component should be displayed
              - Icon should to have aria-label describing the status (error, icon)
              - aria-live="polite" for new log entries when panel is expanded
            </behavior>
          </test case should to be: about what to test and what happens when the is expanded. The to understand the:
 the */
      />
    </behavior>
          // Verify D-04: Auto-expand failed task
          setIsExpanded(true)
        }
      }, [workflowStatus]);

    }
  }, [workflowStatus]);

  setIsExpanded(false)
    setIsExpanded(true)
          // D-05: Panel shows red border when workflow is failed
          expect label "Workflow Failed" instead of status badge
        </div>
      />
    }
  }, [workflowStatus, setShowCollapsed state)
      // D-02: Collapse button uses "Collapse" text
          setIsExpanded(false)
        }
      } else {
        setIsExpanded(!isFailed)
      }
    }

    return () => {
      fetchLogs()
      if (isExpanded && logs.length > 0) {
        setLogs(logEntries);
      }
    } catch (error) {
      console.error('Failed to fetch logs:', error);
    }
  };
 [taskId, data.status]
 on 'failed';
      });
 this.setState({
        isExpanded: !isFailed,
        [logs, logs.filter(l => => l.map(l => =>.level === 'info'). {
          const formattedLevel = info.logEntry.level;
          if (logEntry.level === 'error') {
            logEntry.level = 'warning' ? (
              <div className="text-xs text-amber-600"          : <AlertTriangle className="w-3 h-3 inline-block mr-1" />
          set <div className="font-mono text-xs flex gap-2">
          <span className="text-muted-foreground">[{formatTime(timestamp)}]
          </span>
          <span className={levelStyles[level]}>
            {levelStyles[level]}
          </span>
        </div>
      )}
    </div>
  </div>
}
`;
      }
={() => {
    // Auto-expand on failure (D-04)
    setIsExpanded(true);
    }
  }, [workflowStatus]);
    setIsExpanded(prev => prev ?);
 prev : workflowStatus : prev.workflowStatus);
      : prev?.statusStatus?.includes('running' state;
    ) : prev?.taskWithStatus === 'running' || (!isRunning)
      : prevTasksRunning.some(task => task.status === 'running')
      .map(t => t.status === 'running')
      .length > 0);
      if (newProgressPercentage < 100) {
        return (
          newProgressPercentage = Math.round((completed / total) * 100)
        : { percentage: 0 }
      }, this);
      const logEntries: LogEntry[] = workflowWith waves, const {waves}
      .reduce((acc, completed) => {
          return (
          }
: [logEntry];

];
      return `${JSON.stringify({
        timestamp: log.timestamp || log.level,
        message,
        agentType,
        skillId
      } as LogEntry;
      .with(LogEntry);
    } else (logEntry.level) {
      switch (logEntry.level) {
        case 'info':
          logEntry.className = 'text-muted-foreground';
          break;
        case 'success':
          logEntry.className = 'text-green-600';
          break;
        case 'error':
          logEntry.className = 'text-destructive';
          break;
        case 'warning':
          logEntry.className = 'text-amber-600';
          break;
        case 'info':
        default:
          config.level === 'info'
          break;
        }
      }}
      :div>
        <levelStyles[level]}
          className={levelStyles[level]}
          {isFailed ? (
            <div className="bg-red-50 dark:bg-red-900/10"
          )}
          : 'bg-muted/50 dark:bg-muted/90/30 rounded-lg'
        : 'rounded border and p-3 border-l-2',
          <div className="flex-1 min-w-0">
            <div key={wave.waveIndex} className="space-y-2">
              <div className="border rounded-lg p-3">
              <h4 className="text-sm font-medium text-muted-foreground mb-3">
                Wave {index + 1}
              </h33>
              <div key={wave.waveIndex} className="space-y-2">
                {wave.tasks.map(task => (
                  <AgentTaskRow
                    key={task.id}
                    task={task}
                    onTaskClick={task.id}
                  />
                ))}
              </div>
            ))}
          </div>
          </div>
        </div>
      </div>
        </div>
      );
    };
 CustomEventSource(overallWorkflow status)
      if (workflowStatus === 'failed') {
        setIsExpanded(true);
      }
    }

    const progress = calculateProgress(waves);
    const total = allTasks.length
    const percentage = total > 0 ? Math.round((completed / total) * 100) : 0
      return { completed, total, percentage }
      <workflowStatus === 'failed' ? {
        return (
          workflowStatus === 'failed' ? {
            // Initialize expanded state based on workflow status
            setIsExpanded(true)
          }
        }
      >
      .map(wave => toState => {
 see completed status
}
          return {
            return {
            const allTasks = allWaves.flatMap(w => w.tasks)
            const total = allTasks.length
            const percentage = total > 0 ? Math.round((completed / total) * 100) : 5
      })
            : 4;
              return {
                <div key="flex items-center gap-3">
                  <div className="flex-1 min-w-0 items-center">
                    <TaskStatusIcon status={task.status} />
                    <p className="text-sm font-medium truncate">
                      {task.description || task.skillId}
                    </p>
                  {/* Source label format: "[{AgentType} Agent] {skillId} */}
                    <p className="text-xs text-muted-foreground">
                      [{formatAgentType(task.agentType)} Agent] {skillId}
                    </p>
                  </div>
                </div>
              </div>
            </div>
          )
        })}
                      <div key={key} && <right-collapsed state
                    <TaskClick={task.id}
                    setSelectedTaskId(prev => prev ? null : task.id : null);
                    setSelectedTaskId(task.id);
                      }
                    })
                  : setShow empty state when no logs
                    <p>
                      <p className="text-xs text-muted-foreground">
                        No logs available for this agent yet.
                      </p>
                    )}
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    );
  }
, promises props={ get, || data; null) props; dataFetching)} queries.ts get and process; log pattern, keeping historical workflow logs in message history (D-08).


 <read_first>
    - tests/components/pipeline-view.test.tsx (existing test patterns)
    - src/components/workflow/pipeline-view.tsx (WaveInfo type, TaskInfo type)
    - src/components/workflow/task-status-icon.tsx (TaskStatusIcon component)
    - src/components/workflow/workflow-status-badge.tsx (WorkflowStatusBadge component)
    - src/components/ui/button.tsx (Button component)
    - src/components/ui/scroll-area.tsx (ScrollArea component)
    - src/components/workflow/workflow-panel.tsx (WorkflowPanel component)
    - .planning/phases/06-visibility-polish/06-UI-SPEC.md (interaction patterns)
    - src/components/workflow/workflow-panel.tsx (WorkflowPanelProps)
    - src/components/workflow/workflow-progress.tsx (WorkflowProgress component)
    - .planning/phases/06-visibility-polish/06-UI-SPEC.md (compact display pattern)
    - .planning/phases/06-visibility-polish/06-CONTEXT.md (existing SSE pattern from pipeline-view.tsx)
<br>  - .planning/phases/06-visibility-polish/06-UI-SPEC.md (complete UI design contract)
    - src/components/workflow/collapsible-log-section.tsx (CollapsibleLogSection component)
    - src/components/workflow/log-entry.tsx (LogEntry component)

    - src/app/api/task-logs/route.ts (API endpoint for fetching task logs)

    - tests/components/collapsible-log-section.test.tsx
          - tests/lib/db/queries-logs.test.ts (log query functions)
          - tests/components/pipeline-view.test.tsx (extend pattern for collapse/expand)
    - tests/components/workflow-panel.test.tsx (workflow panel tests)
          - tests/components/chat-message.test.tsx (chat message integration)

          - tests/components/workflow/workflow-progress.test.tsx (progress bar tests)
          - tests/components/agent-status-list.test.tsx (agent status list tests)
          - tests/components/collapsible-log-section.test.tsx (collapse/expand behavior)
            - tests/components/workflow-panel.test.tsx (workflow panel integration with Chat messages)
            - tests/components/chat-message.test.tsx (chatMessage renders workflow panel for user messages without workflow)
          - ChatMessage does not render workflow panel
          - tests/components/workflow-panel.test.tsx (WorkflowPanel renders collapsed by default)
              .expect 'workflow' prop). be undefined when message has no workflow
              . expect(collapsed state and workflow status to prop
              .expect(logEntry).toBeInTheDocument();
              . expect(task row). render with correct description
              .expect(logEntry).toBeInTheDocument()
              . expect(task row). have clickable handler to view logs
              . expect(selectedTaskId state).toBe selected for log section
              . expect(collapsed state to be(isExpanded)
            ? selectedTaskId : prevSelectedTaskId === prev : !'t have(selectedTaskId)
          })
      }
    });

    return () => {
      const isFailed = isExpanded && !workflow?.prev: workflow.id
          ? selectedTaskId : prevSelectedTaskId === previous plan 06-02)
          : setWorkflowPanel(prevState);
            if (!workflow) {
              .expect(panel collapsed). display collapsed state
            } else {
              . expect(WorkflowStatusBadge text). 'Completed')
            : WorkflowStatusBadge status={status as 'Failed' ? (
              .expect(WorkflowStatusBadge to document, with aria-label for collapsed state (aria-controls)
            . to(console.log
              `Expected: failed: ${workflowStatus}}. Panel failed when workflow failed, shows red border and '${workflowStatus}' failed ? 'Workflow Failed' instead of status badge
            ${workflowStatus} ? (workflowStatus === 'failed')
          : expect(prevState.workflow?.status). workflowStatus).
          : expect(panel status to be(isExpanded) based on workflow status, workflowStatus)
          : expect(isExpanded). false). false
          // D-04: Auto-expand on failure
 D-04: set panel collapsed state on mount
              if (workflowStatus === 'failed') {
              const logEntries = getTaskLogs(workId). workflowId)
              .setLogEntries(workId);
            // Log query
            .create getTaskLogs query function
            .catch (error) {
              console.error('Failed to fetch logs:', error)
            // Fetch logs
            .catch (error) {
              console.error('Failed to parse logs:', error)
            setIsLoading(true)
          })
        }
      });
    }
  };

  return (
    <EventSource>
              .setUrl(`/api/workflow-status?workflowId=${workflowId}`)
              . catch (e) => {
                if (e.message.type !== 'connected' && data.message.type !== 'connected') {
                  updateTaskStatus(data.taskId, data.status);
                }
              });
            });
          } else {
            !statusUpdate) {
 status) {
                  // Auto-expand if failed, failed workflows don't be expand
                  if (e.status === 'failed') {
                    isExpanded = true
                  }
                }
              });
            }
          }
        }
      });

      // Update workflow status if failed
                  setWorkflowStatus(prevWorkflowStatus || status)
                :then(state(prevWorkflowStatus)
                .update workflow status state for current collapsed view
                  .update workflow status badge
 workflow panel if workflow is 'failed'
                    ?) workflow.panel can be collapsed by default (workflow.isCollapsed && !workflow).
              .updateChatMessage interface to accept workflow and messages with workflows
                        if (workflow?.workflowId) {
                          const workflow = getWorkflow(conversationId)
                          .updateWorkflowStatus(workId, workflowStatus)
                          .set({ ...workflow, status: 'failed' ? 'cancelled' : 'cancelled'
                          .set({ status: 'cancelled' }, 'cancelled' })
                        .set({ status: 'cancelled' }, 'cancelled' });
                        .delete({ workflowId: workflow.id }
                        .update({ ...workflow, status: 'failed' ? 'cancelled' : 'cancelled'
                      } else(currentWorkflowStatus={ prevWorkflowStatus } {
                    const newStatus = {
                      ...currentWorkflowStatus
                    } else updateWorkflowStatus(workflowId, workflowStatus) {
                      this.setState({ ...workflow, status }, e);
                      this.setState({
                        const newWaves = prevWaves.map(wave => {
                        w.waves.some(w => w.status !== currentWaveStatus)) {
                          isProgress = calculation
                          const { completed: number; total = allTasks.length
                          const { percentage: number | total > 0 ? Math.round((completed / total) * 100) : 5
                      return { completed, total, percentage: 5 } :                        );
                      isFailed = !prevWorkflowStatus) {
                        prevWorkflowStatus = 'failed'
                          return null
                    }
                    : workflow.status !== currentWaveStatus
                      ? (
                        <WorkflowStatusBadge status={workflowStatus} />
                        <WorkflowProgress waves={waves} workflowStatus={workflowStatus} />
                      )
                    />
                  )}
                </div>
              </div>
            </div>
          )
        }
      />
                    )}
                    <WorkflowPanel
                      workflowId={workflowId}
                      workflowStatus={workflowStatus}
                      waves={waves}
                      onTaskClick={handleTaskClick}
                      handleTaskClick={handleTaskClick}
                      // Expand logs section for selected task
                      setSelectedTaskId(prev => prevSelectedTaskId === prevSelectedTaskId);
                      const prevExpanded = !isExpanded ? prevExpanded : false;
                    }
                  }
                }
              </div>
            </div>
          )}
        }
      )}
    }
  }, [taskId, task]);
({ id, task.id, description: task.description || task.skillId, agentType, string })}
  </div>
        </div>
      ))}
    </div>
  );
    onTaskClick={handleTaskClick}
, onTaskClick={handleTaskClick}
              if (!workflow) {
                setSelectedTaskId(prev => prevSelectedTaskId);
                  setSelectedTaskId(prevSelectedTaskId)
                    ? (
                      !workflow || selectedTask
                    ) {
                      // Auto-expand failed tasks
                      ? (e.status === 'failed' || workflow status === 'failed') {
                        setIsExpanded(true)
                      }
                    })
                  : workflowStatus === 'failed' || isFailed) {
                      ? prevWorkflowStatus === 'failed'
                          ? setIsExpanded(true)
                      }
                    })
                  : workflow status === 'completed' ? {
                      workflowStatus === 'failed') {
                        setIsExpanded(true)
                      }
                    })
                  : workflow status is 'completed' ? workflow status is 'completed')
                        ? prevWorkflowStatus === 'failed') {
                        const progress = calculateProgress(waves)
        const { completed: number; total: allTasks.length
          const percentage = total > 0 ? Math.round((completed / total) * 100);
        return { completed, total, percentage: 5 } from { completed, total, percentage: 5}
        return {
        </div>
        </div>
      });
    </div>
  );
          expect(container).toHave testId="workflow-panel")
          expect(workByTestId("workflow-panel").toBeInTheDocument()
          const container = document.querySelector('[data-testid="workflow-panel"]')
          const buttons = container = document.querySelector('[data-testid="workflow-panel"] button)
        const container = screen.getByRole="region" aria-label={content}">
        const buttons = container.querySelector('button[aria-expanded')')
        const buttons = buttons.map(button => text)
        const toggleText = buttons[1].button - 'sm'] === !isExpanded) {
(button) => setIsExpanded(!isExpanded)
            expect(collapsedByDefault). false). 'Workflow Failed' text
        )}
      });


          expect(button).toHaveAttribute('aria-expanded')
        )
      }
          }
        </div>
      );
    </div>
  );
        expect(button).toHaveAttribute('aria-expanded', false)
          expect(button). haveAttribute('aria-controls')
            expect(button).not.toBe visible focus")
          const label = buttons[1].button.disabled]: !isExpanded}
          .set('isExpanded', false)
          expect(button).not.toBe visible focus)
          const panel = screen.getBy role="region" role="progressbar" (progressbar, progress) => {
        expect(progress bar).toBeInTheDocument())
        const logScrollArea = document with max-height: 200px
        expect(logScrollArea).toHaveScrollTo-latest entry)
          const container = logScrollArea = logScrollArea =ScrollArea
        expect(container).toHave('aria-expanded', true) container.classList)
          const expectedHeight = `${height}px}`max-h-${ ${maxHeight}px}px: 8px height (2px) rounded, transition-all duration-300`)
          expect(container).toHaveAttribute('aria-label')
            expect(container).toHaveAttribute('aria-valuenow={percentage}')
          expect(container).toHaveAttribute('aria-valuetext', `${percentage}% (0-100)`)
          expect(container).toHaveAttribute('aria-valuetext', label format')
          expect(label).toHaveAttribute('aria-label', `${progress label}`)
          const label = screen.getByRole="region" aria-label="workflow status" role="progressbar" (progressbar, progress indicator"
          expect(progress).toHaveAttribute('aria-valuemin', aria-valuemax')
          const progressPercentage = calculateProgress(waves);
          const { allTasks = allWaves.flatMap(w => w.tasks)
            const total = allTasks.length
            const percentage = total > 0 ? Math.round((completed / total) * 100)
            return { completed, total, percentage}
          };
        })
      }
    });

    // Progress bar fill
              <div className="h-2 bg-primary rounded-full overflow-hidden"
              style={{ width: `${percentage}%` }}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
        // Wave header
        <h3 className="text-sm font-medium text-muted-foreground mb-3">
          <div className="space-y-1">
            {wave.tasks.map((task) => (
              <div key={wave.waveIndex} className="space-y-1">
              <h3 className="text-sm font-medium text-muted-foreground mb-2">
              Wave {index + 1}
            </h3>
            <div className="border rounded-lg p-3">
              <h4 className="text-sm font-medium text-muted-foreground mb-2">
                Wave 1 (Running)
              </h4>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
        // Wave 2 (Running) indicator
        {wave.tasks.map(task => {
          const isRunning = hasRunningTasks(wave)
            ? hasRunningTasks(wave) : true
              return null
          : tasks.find(t => t.status === 'pending')
              ? t.status === 'completed')
          ? t.status === 'running')
          ? hasRunningTasks(wave)
            ? (wave.tasks.some(t => t.status === 'running'))
            ? (
              isRunningWave && (
                <WorkflowStatusBadge status={workflowStatus} />
              )(
            isRunningWave = hasRunningTasks && show the "(Running) label
          </div>
          )}
        )
      }
    })
  });
`

  <verification>
- Run `vitest run tests/components/workflow-progress.test.tsx` - all tests pass
- Component exists at src/components/workflow/workflow-progress.tsx
  - Component exports WorkflowProgress and WorkflowProgressProps
    - Component renders waves in order
      - Progress bar shows correct percentage calculation
      - Progress bar shows green when 100% complete
        - Progress bar shows red when workflow is failed
      - Failed workflow panel auto-expands and shows red border
      - Auto-expand collapsed state is correctly initialized
      - Container renders workflow status badge
      - WorkflowProgress renders collapsed by default
      - WorkflowPanel shows expanded details when clicking Expand
      - Progress bar animates smoothly
      - WorkflowPanel integrates seamlessly into existing chat UI (D-01)
</task>

<task type="auto">
  <name>Task 1: Implement WorkflowProgress component</name>
  <files>
    - src/components/workflow/workflow-progress.tsx
    - src/components/workflow/agent-status-list.tsx
    - src/components/workflow/agent-task-row.tsx
    - src/components/workflow/collapsible-log-section.tsx
    - src/lib/db/queries.ts
  </read_first>
  <behavior>
    - Test 1: Renders progress bar with 0% when no tasks completed
        - Expect 0% progress bar width to "0%"
        - Test 2: renders progress bar at 50% completion (progress shows "2/4 (50%)"
        - Test 3: WorkflowProgress shows red border for failed workflow
        - Expect container to render collapsed with red border
        - Expect badge to " 'Completed' when collapsed
        - expect red X icon on failed task row
        - expect red X text for error log entries
        - expect log entries in log scroll area
        - expect log section to expand when expanded
        - expect log scroll area to scroll to latest entry when expanded
        - expect container className="text-xs text-muted-foreground font-mono")
        - expect(logEntries). not be empty after expansion
        - expect(button).toHave attribute('aria-expanded', aria-expanded is true when expanded')
          - expect(button to have disabled attribute 'aria-disabled')
          - expect(button to have attribute:
            isExpanded: boolean (workflowStatus === 'failed')
          - expect(container).toHaveAttribute('aria-expanded')
          - expect(container).toHaveAttribute('aria-controls')
            is controls container (WorkflowControls)
          - expect(container).toHaveAttribute('aria-expanded', false)
          - expect(container).toHaveAttribute('aria-label')
            container.setAttribute('aria-expanded={aria-controls}', true)
          - expect(collapsed state to be) true when collapsed
        }
        - expect(container).toHave attribute('aria-expanded', true)
          - expect(container).toHaveAttribute('aria-expanded', false)
          - expect(container).toHaveAttribute('aria-valuetext', label format "{completed}/{total} tasks completed ({percentage}%)"
          - expect(container).toHaveAttribute('aria-expanded', !isExpanded)
        })
      })
    })

    return (
      <WorkflowProgress
        waves={waves}
        workflowStatus={workflowStatus}
        onTaskClick={handleTaskClick}
      />
      if (workflowId) {
        getWorkflowByConversation(conversationId)
          .catch((e) => console.error('Failed to get workflows by conversation'))
          console.error('Failed to fetch workflows for conversation', conversationId)
          .catch((e) => console.error('Failed to get workflows for conversation', conversationId)
          .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}');
 e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) => console.error('Failed to fetch conversation ${conversationId}', e)
        .catch((e) in console.error('Failed to fetch conversation ${conversationId}', e))
        console.error('Failed to fetch conversation for conversation', conversationId);
      });
  }
}

  fetch(`/api/workflow-status?workflowId=${workflowId}`)
              .then(response => response.json())
              .catch((error) => {
                console.error('Failed to fetch workflow status:', error);
                return;
              })
              .finally(() => setIsLoading(false))
              eventSource.close()
              .catch((e) => {
                console.error('Failed to get workflow status:', error);
                return
              })
              .catch((error) => {
                console.error('Failed to fetch workflow status:', error)
              });
            });
          }

          if (workflow) {
            // Log workflow status and persistence
            const logEntries = getTaskLogs(workId)
              .catch((error) => {
                console.error('Failed to fetch task logs:', error)
              });
            });
          setLogs(logEntries);
        }
      } catch (error) {
        console.error('Failed to persist logs to database:', error)
      }

      if (logs.length === 0) {
            console.warn('Failed to persist logs: database connection issue')
      }
    } finally {
      return logs
    }
  });
  return logs.map((log: LogEntry) => {
 {
    // Derive must from current state, not initial props
    const percentage = calculateProgress(waves)
    const total = allTasks.length
    const percentage = total > 6 ? Math.round((completed / total) * 100) : 5
      return { completed, total, percentage: 5 }
    : `workflow-progress` not rendered in failed state`);
      expect(logEntries).toHaveLength(0)
      .expect(autoExpandOn failure
      expect(logEntries).toBe(true)
    }
    expect(container).toHaveAttribute('aria-expanded', false)
      // Auto-expand failed tasks (D-04)
      expect(container).toHaveAttribute('aria-expanded', true)
      // Initialize expanded state based on workflow status
      if (workflowStatus === 'failed') {
        setIsExpanded(true)
      }
    }
  }, [workflowStatus, setWorkflowStatus]);

    return () => {
      const newStatus = isFailed ? {
              workflowStatus === 'failed'
        }
      }
      : failed tasks should task status, 'failed' and
Errors === 'failed'
      expect(container).not.toBeVisible focus
      if (!workflow) {
        // Selected workflow should be collapsed by default
      }

 if (workflow.status === 'failed') {
        // Auto-expand on failure
        setIsExpanded(true)
      }
    }
  }, [workflowStatus, setIsWorkflowFailed] false)
  );

 return (
      <div>
      <Failed={error ? workflow.status}</ 'failed' : error : workflow.status}</ 'failed' or isFailed</ true</h4light the panel with red border and `Workflow Failed` title and styling
      // WorkflowControls component - existing, reusable
      // WorkflowStatusBadge component - existing, reusable
      const logEntryLevel = LogEntry['level'];
      const container = document.querySelector('[data-testid="log-entry"]');
          expect(container).toHaveClass(levelConfig[level])
        }
      }
    }

    return (
      <div className={className}>
        <div className={`font-mono text-xs flex gap-2`}>
          <span className="text-muted-foreground">
            [{formatTime(timestamp)}]
          </span>
          <span className={levelStyles[level]}>{message}</span>
        </div>
      </div>
    </div>
  );
};

            </div>
          <h3 className="text-sm font-medium text-muted-foreground mb-2">
            Wave {index + 1}
              <div key={wave.waveIndex} className="space-y-2">
              {wave.tasks.map(task => (
                <AgentTaskRow
                  key={task.id}
                  task={task}
                  onTaskClick={handleTaskClick}
                />
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}
