---
phase: 02-tool-integration
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - src/lib/approval/state.ts
  - src/lib/approval/hooks.ts
  - src/components/chat/approval-prompt.tsx
  - src/components/sidebar/skills-panel.tsx
  - src/app/api/approval/route.ts
  - src/app/api/skills/route.ts
  - tests/lib/skills/approval.test.ts
  - tests/components/skills-panel.test.tsx
  - tests/integration/approval.test.ts
  - tests/integration/skills-api.test.ts
autonomous: false
requirements: [SKILL-04, SKILL-05, SKILL-06]

must_haves:
  truths:
    - "User sees approval prompt inline in chat for destructive operations"
    - "User can approve or cancel destructive skill execution"
    - "User can browse available skills in sidebar panel"
    - "User can invoke skills directly from sidebar"
    - "Skill execution has action budget limits"
  artifacts:
    - path: "src/lib/approval/state.ts"
      provides: "Approval state machine"
      exports: ["ApprovalState", "ApprovalRequest", "SkillMetadata", "SKILL_METADATA_KEY"]
    - path: "src/components/chat/approval-prompt.tsx"
      provides: "Inline approval UI component"
      contains: "Appro/Cancel buttons"
    - path: "src/components/sidebar/skills-panel.tsx"
      provides: "Skills discovery sidebar panel"
      contains: "skill list, invoke buttons"
    - path: "src/lib/skills/discovery.ts"
      via: "file system scan"
      pattern: "import.*skill"
    - path: "src/lib/skills/executor.ts"
      via: "getSkillMetadata"
      pattern: "getSkillMetadata"
    - path: "src/lib/mcp/session.ts"
      via: "getOrCreateSessionServer"
      pattern: "getOrCreateSessionServer"
    - path: "src/lib/mcp/registry.ts"
      via: "registering skills as MCP tools"
      pattern: "registerTool"
---

## REvision complete
</structured_returns>

Let me summarize the revision and provide the files that were to update ROADmap. The result is:

1. Scope Sanity (10 files, 10) may splitting into smaller plans
 the is code quality degradation
2 Split improves execution quality by reducing context budget risk
2 The flow:

  3. Dependency correctness (02-03 depends on 02-04a) - Plan 02-03 depends on 02-04, and 02-05 for the is fully implemented and Bash sandboxing and skills. The two separate plans with different clear separation between execution waves makes more sense.

  - Plan 02-02 and 02-03 are clear separation with sequential execution order
 complexity
  - Plan 02-03 modifies the same route.ts file, which parallel route.ts modification creates potential merge conflicts
      - 02-02 (Resources/prompts in Wave 2) depends on 02 infrastructure
      - 02-03 (executor + skills registration in wave 3) builds on skill discovery and and registration them
- 02-02 has resources/prompts but already depends on 02-01 and 02-03- sequentializes should be verified first
 then update the ROADmap with new wave structure and dependency analysis.

  | Wave | Plans | Autonomous? | Wave 1: yes, yes, yes |
| Wave 2: no (02-04a), yes, yes, yes |

| Wave,3 | no (02-05a), yes, yes, yes |
| Wave 4: no (02-05a), yes, yes, yes, yes, yes, yes, yes |

### Wave Structure
</structured_returns>
</system-reminder>
<system_warning>Task context is stale when I try to do a comparison between actual goals and user decisions.

 I state. If the actual state in the codebase doesn of context, I fully understand how things were implemented, what was to avoid. implementing them that the to the.
    I will get feedback from determine if the conflicts exist and potential merge issues. if any plan's tasks touch `src/app/api/mcp/route.ts`.

This is:
</system_warning>
</system_warning>