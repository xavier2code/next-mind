---
phase: 02-tool-integration
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - src/lib/skills/executor.ts
  - src/lib/skills/discovery.ts
  - src/lib/approval/types.ts
  - src/lib/approval/state.ts
  - src/components/chat/approval-prompt.tsx
  - src/components/sidebar/skills-panel.tsx
  - src/app/api/skills/route.ts
  - src/app/api/approval/route.ts
  - tests/lib/skills/executor.test.ts
  - tests/lib/skills/discovery.test.ts
  - tests/lib/approval/state.test.ts
  - tests/components/approval-prompt.test.tsx
  - tests/components/skills-panel.test.tsx
autonomous: false
requirements: [SKILL-04, SKILL-05, SKILL-06]

must_haves:
  truths:
    - "User can see available skills in a sidebar panel"
    - "User can invoke skills directly from the sidebar"
    - "Destructive skills show inline approval prompt in chat"
    - "User can approve or cancel destructive operations"
    - "Skill execution respects timeout limits"
    - "Skill execution is logged for audit"
  artifacts:
    - path: "src/lib/skills/executor.ts"
      provides: "Skill execution engine with timeout and approval support"
      exports: ["executeSkill", "SkillExecutor"]
    - path: "src/lib/skills/discovery.ts"
      provides: "Auto-discovery of skills from src/skills/ directory"
      exports: ["discoverSkills", "getSkillById"]
    - path: "src/lib/approval/types.ts"
      provides: "Approval request and state types"
      exports: ["ApprovalRequest", "ApprovalStatus"]
    - path: "src/lib/approval/state.ts"
      provides: "In-memory approval state management"
      exports: ["ApprovalStateMachine", "createApprovalRequest"]
    - path: "src/components/chat/approval-prompt.tsx"
      provides: "Inline approval UI component for destructive operations"
      contains: "ApprovalPrompt"
    - path: "src/components/sidebar/skills-panel.tsx"
      provides: "Sidebar panel for skill discovery and browsing"
      contains: "SkillsPanel"
    - path: "src/app/api/skills/route.ts"
      provides: "API endpoint for listing and discovering skills"
      exports: ["GET"]
    - path: "src/app/api/approval/route.ts"
      provides: "API endpoint for approval flow"
      exports: ["POST"]
  key_links:
    - from: "src/lib/skills/executor.ts"
      to: "src/lib/approval/state.ts"
      via: "approval check for destructive skills"
      pattern: "requiresApproval"
    - from: "src/lib/skills/executor.ts"
      to: "src/lib/skills/discovery.ts"
      via: "skill lookup by ID"
      pattern: "getSkillById"
    - from: "src/components/sidebar/skills-panel.tsx"
      to: "src/app/api/skills/route.ts"
      via: "fetch skills list"
      pattern: "fetch.*api/skills"
    - from: "src/components/chat/approval-prompt.tsx"
      to: "src/app/api/approval/route.ts"
      via: "submit approval decision"
      pattern: "fetch.*api/approval"
---

<objective>
Implement skill execution engine with action budgets and approval gates, inline approval UI for destructive operations, and a sidebar panel for skill discovery and browsing.

Purpose: Enable users to safely execute skills with proper oversight for destructive operations and provide an intuitive interface to discover and invoke skills.
Output: Working skill execution system with approval flow and discovery UI.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-integration/02-RESEARCH.md
@.planning/phases/02-tool-integration/02-04-SUMMARY.md

<interfaces>
<!-- Types from 02-04 that this plan depends on -->

From src/lib/skills/types.ts:
```typescript
export interface SkillMetadata {
  id: string;
  name: string;
  description: string;
  version: string;
  category: SkillCategory;
  tags: string[];
  inputSchema: Record<string, z.ZodTypeAny>;
  requiresApproval: boolean;
  destructiveActions: string[];
  dependencies: string[];
  timeout: number;
}

export interface SkillContext {
  userId: string;
  sessionId: string;
  conversationId?: string;
  previousResults: Map<string, SkillResult>;
}

export interface SkillResult {
  success: boolean;
  data?: unknown;
  error?: string;
  metadata?: Record<string, unknown>;
}

export type SkillFunction = (
  input: unknown,
  context: SkillContext
) => Promise<SkillResult>;

export type SkillCategory = 'file' | 'data' | 'web' | 'system' | 'custom';
```

From src/lib/skills/decorator.ts:
```typescript
export const SKILL_METADATA_KEY = Symbol('skill:metadata');
export function skill(metadata: SkillMetadata): MethodDecorator;
export function getSkillMetadata(target: unknown, propertyKey: string): SkillMetadata | undefined;
export function skillToMcpTool(skillFn: SkillFunction, metadata: SkillMetadata): McpTool;
```

From src/lib/audit.ts:
```typescript
export function logAudit(entry: AuditEntry): Promise<void>;
export function getClientInfo(request: NextRequest): { ipAddress: string; userAgent: string };
```

From src/components/sidebar/sidebar.tsx:
```typescript
interface SidebarProps {
  open: boolean;
  onToggle: () => void;
}
// Uses: Button, ScrollArea, SearchInput patterns
// Style: bg-zinc-100, border-r border-zinc-200, w-64 when open
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skill discovery and executor</name>
  <files>src/lib/skills/discovery.ts, src/lib/skills/executor.ts, tests/lib/skills/executor.test.ts, tests/lib/skills/discovery.test.ts</files>
  <read_first>
    - src/lib/skills/types.ts (SkillMetadata, SkillFunction, SkillResult)
    - src/lib/skills/decorator.ts (getSkillMetadata, SKILL_METADATA_KEY)
    - src/skills/file-processing.ts (example skill implementation)
    - src/lib/audit.ts (logAudit for execution logging)
  </read_first>
  <behavior>
    - Test 1: discoverSkills returns all skills from src/skills/ directory
    - Test 2: getSkillById returns correct skill metadata
    - Test 3: executeSkill runs skill and returns result
    - Test 4: executeSkill respects timeout limit
    - Test 5: executeSkill throws error for unknown skill
    - Test 6: executeSkill validates input against schema
  </behavior>
  <action>
    Create src/lib/skills/discovery.ts with:

    1. DiscoveredSkill interface:
       - metadata: SkillMetadata
       - execute: SkillFunction
       - sourceFile: string

    2. discoverSkills(): DiscoveredSkill[]
       - Scan src/skills/ directory for .ts files
       - Import each module and extract decorated methods
       - Use getSkillMetadata() to retrieve metadata
       - Return array of DiscoveredSkill objects
       - Cache results in module-level Map for performance
       - Include error handling for malformed skill files

    3. getSkillById(id: string): DiscoveredSkill | undefined
       - Query the discovered skills cache
       - Return matching skill or undefined

    4. getSkillsByCategory(category: SkillCategory): DiscoveredSkill[]
       - Filter discovered skills by category

    5. clearCache(): void
       - Clear the discovery cache (for testing/hot-reload)

    Create src/lib/skills/executor.ts with:

    1. ExecutionOptions interface:
       - timeout?: number (override skill default)
       - skipApproval?: boolean (for testing)
       - onApprovalRequired?: (request: ApprovalRequest) => Promise<boolean>

    2. SkillExecutor class:
       - constructor(private registry: Map<string, DiscoveredSkill>)

       - async execute(skillId: string, input: unknown, context: SkillContext, options?: ExecutionOptions): Promise<SkillResult>
         - Get skill from registry
         - Validate input against skill.inputSchema using Zod
         - If skill.requiresApproval and !options.skipApproval:
           - Create ApprovalRequest
           - Call options.onApprovalRequired if provided
           - If not approved, return { success: false, error: 'Approval denied' }
         - Execute skill with timeout wrapper (Promise.race with setTimeout)
         - Log execution to audit (userId, skillId, success, duration)
         - Return result

       - async executeWithTimeout(fn: SkillFunction, input: unknown, context: SkillContext, timeoutMs: number): Promise<SkillResult>
         - Wrap execution in Promise.race with timeout
         - On timeout, return { success: false, error: 'Skill execution timed out' }

    3. createSkillExecutor(discoveredSkills?: DiscoveredSkill[]): SkillExecutor
       - Create executor with discovered skills
       - Default to discoverSkills() if not provided

    4. executeSkill(skillId: string, input: unknown, context: SkillContext, options?: ExecutionOptions): Promise<SkillResult>
       - Convenience function using global executor instance

    Export all types and functions.
  </action>
  <verify>
    <automated>npm test -- --run tests/lib/skills/executor.test.ts tests/lib/skills/discovery.test.ts</automated>
  </verify>
  <done>
    - src/lib/skills/discovery.ts exports discoverSkills, getSkillById, getSkillsByCategory
    - src/lib/skills/executor.ts exports SkillExecutor, executeSkill, createSkillExecutor
    - discoverSkills() finds all skills in src/skills/ directory
    - executeSkill() validates input, handles timeout, logs to audit
    - Tests pass: discovery, execution, timeout handling
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create approval flow system</name>
  <files>src/lib/approval/types.ts, src/lib/approval/state.ts, tests/lib/approval/state.test.ts</files>
  <read_first>
    - src/lib/skills/types.ts (SkillMetadata for context)
    - src/lib/audit.ts (logAudit pattern)
  </read_first>
  <behavior>
    - Test 1: createApprovalRequest generates valid request with pending status
    - Test 2: ApprovalStateMachine transitions from pending to approved
    - Test 3: ApprovalStateMachine transitions from pending to rejected
    - Test 4: ApprovalStateMachine rejects invalid transitions (approved to pending)
    - Test 5: ApprovalStateMachine expires requests after timeout
    - Test 6: getPendingApprovals returns only pending requests
  </behavior>
  <action>
    Create src/lib/approval/types.ts with:

    1. ApprovalStatus type:
       - 'pending' | 'approved' | 'rejected' | 'expired'

    2. ApprovalRequest interface:
       - id: string (UUID)
       - skillId: string
       - skillName: string
       - action: string (description of what will happen)
       - details: string (additional context)
       - input: unknown (the input that will be passed to skill)
       - status: ApprovalStatus
       - createdAt: Date
       - expiresAt: Date
       - userId: string
       - sessionId: string

    3. ApprovalDecision interface:
       - requestId: string
       - approved: boolean
       - reason?: string

    Create src/lib/approval/state.ts with:

    1. ApprovalStateMachine class:
       - private requests: Map<string, ApprovalRequest>
       - private readonly DEFAULT_TIMEOUT_MS = 5 * 60 * 1000 (5 minutes)

       - createRequest(params: Omit<ApprovalRequest, 'id' | 'status' | 'createdAt' | 'expiresAt'>): ApprovalRequest
         - Generate UUID for id
         - Set status to 'pending'
         - Set createdAt to now
         - Set expiresAt to now + DEFAULT_TIMEOUT_MS
         - Store in requests map
         - Return request

       - approve(requestId: string): ApprovalRequest | null
         - Get request from map
         - If not found or not pending, return null
         - Update status to 'approved'
         - Log audit entry
         - Return updated request

       - reject(requestId: string, reason?: string): ApprovalRequest | null
         - Get request from map
         - If not found or not pending, return null
         - Update status to 'rejected'
         - Log audit entry with reason
         - Return updated request

       - getRequest(requestId: string): ApprovalRequest | undefined
         - Return request from map

       - getPendingApprovals(userId: string): ApprovalRequest[]
         - Filter requests by userId and status === 'pending'
         - Exclude expired requests
         - Return list

       - cleanup(): void
         - Remove expired requests older than 1 hour
         - Called periodically or on access

    2. Global approval state instance:
       - export const approvalState = new ApprovalStateMachine()

    3. createApprovalRequest helper:
       - Wrapper around approvalState.createRequest

    Export all types and functions.
  </action>
  <verify>
    <automated>npm test -- --run tests/lib/approval/state.test.ts</automated>
  </verify>
  <done>
    - src/lib/approval/types.ts exports ApprovalStatus, ApprovalRequest, ApprovalDecision
    - src/lib/approval/state.ts exports ApprovalStateMachine, approvalState, createApprovalRequest
    - Approval state machine handles pending/approved/rejected/expired transitions
    - Tests pass: state transitions, expiration, cleanup
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create approval and skills API endpoints</name>
  <files>src/app/api/skills/route.ts, src/app/api/approval/route.ts, tests/integration/skill-approval.test.ts</files>
  <read_first>
    - src/app/api/mcp/route.ts (API route pattern, auth, audit logging)
    - src/lib/skills/discovery.ts (discoverSkills, getSkillById)
    - src/lib/skills/executor.ts (executeSkill)
    - src/lib/approval/state.ts (approvalState)
    - src/auth.ts (auth session pattern)
  </read_first>
  <behavior>
    - Test 1: GET /api/skills returns list of discovered skills
    - Test 2: GET /api/skills?category=file filters by category
    - Test 3: POST /api/approval with approve decision updates state
    - Test 4: POST /api/approval with reject decision updates state
    - Test 5: Unauthenticated requests return 401
    - Test 6: Invalid approval ID returns 404
  </behavior>
  <action>
    Create src/app/api/skills/route.ts with:

    1. GET endpoint:
       - Authenticate via auth() session
       - Return 401 if not authenticated
       - Get optional category query param
       - If category: call getSkillsByCategory(category)
       - Else: call discoverSkills()
       - Return JSON with skills array (metadata only, not execute functions)
       - Log audit for skills list access

       Response format:
       {
         skills: Array<{
           id: string;
           name: string;
           description: string;
           version: string;
           category: string;
           tags: string[];
           requiresApproval: boolean;
           destructiveActions: string[];
         }>
       }

    Create src/app/api/approval/route.ts with:

    1. GET endpoint (list pending approvals):
       - Authenticate via auth() session
       - Return 401 if not authenticated
       - Get pending approvals for current user
       - Return JSON with approvals array

    2. POST endpoint (submit approval decision):
       - Authenticate via auth() session
       - Return 401 if not authenticated
       - Parse body: { requestId: string; approved: boolean; reason?: string }
       - Validate requestId exists and belongs to user
       - If approved: call approvalState.approve(requestId)
       - If not approved: call approvalState.reject(requestId, reason)
       - Log audit for approval decision
       - Return updated approval request

       Response format:
       {
         success: boolean;
         request?: ApprovalRequest;
         error?: string;
       }

    Export both endpoints.
  </action>
  <verify>
    <automated>npm test -- --run tests/integration/skill-approval.test.ts</automated>
  </verify>
  <done>
    - GET /api/skills returns list of discovered skills
    - GET /api/skills?category=X filters by category
    - GET /api/approval returns pending approvals for user
    - POST /api/approval handles approve/reject decisions
    - All endpoints require authentication
    - Tests pass: API responses, auth, error handling
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create approval prompt component</name>
  <files>src/components/chat/approval-prompt.tsx, tests/components/approval-prompt.test.tsx</files>
  <read_first>
    - src/lib/approval/types.ts (ApprovalRequest)
    - src/components/ui/button.tsx (Button component)
    - src/app/api/approval/route.ts (POST endpoint for decisions)
  </read_first>
  <behavior>
    - Test 1: Renders skill name and action description
    - Test 2: Approve button calls onApprove callback
    - Test 3: Cancel button calls onReject callback
    - Test 4: Shows loading state while processing
    - Test 5: Disables buttons while loading
    - Test 6: Displays details text
  </behavior>
  <action>
    Create src/components/chat/approval-prompt.tsx with:

    1. ApprovalPromptProps interface:
       - request: ApprovalRequest
       - onApprove: () => Promise<void>
       - onReject: () => Promise<void>

    2. ApprovalPrompt component:
       - Use useState for isLoading state
       - Container div with classes:
         - "border rounded-lg p-4 my-2"
         - "bg-amber-50 dark:bg-amber-950/30" (warning color)
       - Header with warning icon (AlertTriangle from lucide-react)
       - Text: "Approval Required" in amber-600 color
       - Skill name in bold, followed by action description
       - Details in smaller, muted text
       - Two buttons:
         - "Cancel" (variant="outline", calls onReject)
         - "Approve" (variant="default", calls onApprove)
       - Both buttons disabled when isLoading
       - Approve button shows "Approving..." when loading

    3. useApprovalDecision hook (optional convenience):
       - Takes requestId, approved, reason
       - Calls POST /api/approval
       - Returns { submit, isLoading, error }

    Export ApprovalPrompt component and hook.
  </action>
  <verify>
    <automated>npm test -- --run tests/components/approval-prompt.test.tsx</automated>
  </verify>
  <done>
    - ApprovalPrompt renders approval request details
    - Buttons trigger onApprove/onReject callbacks
    - Loading state disables buttons and shows feedback
    - Uses warning color scheme (amber)
    - Tests pass: rendering, interactions, loading states
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create skills sidebar panel</name>
  <files>src/components/sidebar/skills-panel.tsx, tests/components/skills-panel.test.tsx</files>
  <read_first>
    - src/components/sidebar/sidebar.tsx (sidebar layout patterns)
    - src/components/sidebar/conversation-list.tsx (list rendering patterns)
    - src/app/api/skills/route.ts (GET endpoint for skills)
    - src/lib/skills/types.ts (SkillCategory)
  </read_first>
  <behavior>
    - Test 1: Renders skills list from API
    - Test 2: Shows loading state while fetching
    - Test 3: Shows error state on fetch failure
    - Test 4: Filters skills by search query
    - Test 5: Groups skills by category
    - Test 6: Shows approval badge for requiresApproval skills
    - Test 7: Clicking skill invokes onSkillSelect callback
  </behavior>
  <action>
    Create src/components/sidebar/skills-panel.tsx with:

    1. SkillsPanelProps interface:
       - onSkillSelect?: (skillId: string) => void
       - className?: string

    2. SkillItem component:
       - Props: skill metadata, onSelect callback
       - Shows skill name, description (truncated)
       - Shows category badge (colored pill)
       - Shows approval badge if requiresApproval (shield icon)
       - Hover effect for clickability
       - OnClick calls onSelect(skill.id)

    3. SkillsPanel component:
       - Use useState for skills, isLoading, error, searchQuery
       - Use useEffect to fetch skills on mount from GET /api/skills
       - Filter skills by searchQuery (name, description, tags)
       - Group skills by category using reduce
       - Render structure:
         - Header: "Skills" title
         - Search input (reuse SearchInput pattern)
         - ScrollArea for skills list
         - Categories as collapsible sections
         - Each skill as SkillItem
       - Loading state: skeleton/spinner
       - Error state: error message with retry button
       - Empty state: "No skills found"

    4. Category colors:
       - file: blue
       - data: green
       - web: purple
       - system: red
       - custom: gray

    5. useSkills hook (optional convenience):
       - Fetches skills from API
       - Returns { skills, isLoading, error, refetch }

    Export SkillsPanel and SkillItem components.
  </action>
  <verify>
    <automated>npm test -- --run tests/components/skills-panel.test.tsx</automated>
  </verify>
  <done>
    - SkillsPanel fetches and displays skills from API
    - Skills grouped by category with colored badges
    - Search filters skills by name/description/tags
    - Approval badge shown for destructive skills
    - Loading and error states handled
    - Tests pass: rendering, filtering, interactions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what_built>Complete skill execution system with:
  - Skill discovery and executor (src/lib/skills/discovery.ts, executor.ts)
  - Approval flow system (src/lib/approval/types.ts, state.ts)
  - API endpoints (src/app/api/skills/route.ts, src/app/api/approval/route.ts)
  - Approval prompt component (src/components/chat/approval-prompt.tsx)
  - Skills sidebar panel (src/components/sidebar/skills-panel.tsx)
  </what_built>
  <how_to_verify>
    1. Run `npm test -- --run` to verify all tests pass
    2. Run `npm run build` to verify TypeScript compiles
    3. Start dev server: `npm run dev`
    4. Navigate to http://localhost:3000
    5. Verify skills panel appears in sidebar (may need to add to layout)
    6. Verify skills list loads and shows categories
    7. Test search filtering in skills panel
    8. Verify approval prompt styling (check component in isolation if needed)
    9. Check API endpoints with curl:
       - curl http://localhost:3000/api/skills (authenticated)
       - curl -X POST http://localhost:3000/api/approval -d '{"requestId":"test","approved":true}'
  </how_to_verify>
  <resume_signal>Type "approved" if verification passes, or describe issues found</resume_signal>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `npm test -- --run` - all tests pass
2. Run `npm run build` - TypeScript compiles without errors
3. Verify skill discovery finds all skills in src/skills/ directory
4. Verify executor handles timeout and approval checks
5. Verify approval state machine transitions correctly
6. Verify API endpoints require authentication
7. Verify UI components render correctly
8. Check audit logging for skill execution and approval decisions
</verification>

<success_criteria>
- Skill executor validates input, handles timeout, logs execution
- Skill discovery auto-finds skills from src/skills/ directory
- Approval flow handles pending/approved/rejected/expired states
- Inline approval prompt appears for destructive operations
- Skills panel shows skills grouped by category
- Requirements SKILL-04, SKILL-05, SKILL-06 satisfied
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-integration/02-05-SUMMARY.md`
</output>
