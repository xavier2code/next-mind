---
phase: 12-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - .gitignore
autonomous: true
requirements:
  - TINF-01

must_haves:
  truths:
    - "Playwright config reads PLAYWRIGHT_BASE_URL env var for baseURL"
    - "webServer section is omitted when PLAYWRIGHT_BASE_URL is set (Docker target)"
    - "webServer starts npm run dev when PLAYWRIGHT_BASE_URL is not set (local dev)"
    - "A setup project is defined matching auth.setup.ts"
    - "The chromium project depends on setup and loads .auth/user.json storageState"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Env-driven Playwright configuration with setup project pattern"
      contains: "PLAYWRIGHT_BASE_URL"
    - path: ".gitignore"
      provides: "Excludes .auth/ directory from version control"
      contains: ".auth/"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/auth.setup.ts"
      via: "setup project testMatch pattern"
      pattern: "auth\\.setup\\.ts"
    - from: "playwright.config.ts"
      to: ".auth/user.json"
      via: "storageState reference in chromium project"
      pattern: "storageState.*\\.auth"
---

<objective>
Update Playwright configuration to support env-driven baseURL switching between local dev and Docker environments, and add the setup project pattern for auth session reuse.

Purpose: Without env-driven baseURL, E2E tests cannot target a Docker container. Without the setup project, every test would need to perform login independently (slow and fragile).
Output: Updated playwright.config.ts with PLAYWRIGHT_BASE_URL support and setup project definition; .gitignore updated to exclude .auth/ directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-infrastructure/12-CONTEXT.md
@.planning/phases/12-test-infrastructure/12-RESEARCH.md
</context>

<interfaces>
<!-- Current playwright.config.ts exports that this plan modifies -->
From playwright.config.ts:
```typescript
// Current: hardcoded baseURL, single chromium project, always-on webServer
export default defineConfig({
  testDir: './e2e',
  use: { baseURL: 'http://localhost:3000', trace: 'on-first-retry' },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: { command: 'npm run dev', url: 'http://localhost:3000', reuseExistingServer: !process.env.CI, timeout: 120 * 1000 },
});
```

From .gitignore (current state - no .auth/ entry):
```
test-results/
playwright-report/
```

Expected new project structure:
```
projects: [
  { name: 'setup', testMatch: /auth\.setup\.ts/ },
  { name: 'chromium', use: { ...devices['Desktop Chrome'], storageState: '.auth/user.json' }, dependencies: ['setup'] },
]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Playwright config with env-driven baseURL, setup project, and conditional webServer</name>
  <files>playwright.config.ts, .gitignore</files>
  <read_first>
    - playwright.config.ts (current config to modify)
    - .gitignore (to add .auth/ exclusion)
  </read_first>
  <action>
Per D-01 (storageState reuse with setup project pattern) and TINF-01 (env-driven baseURL):

1. Rewrite `playwright.config.ts` with these exact changes:
   - Extract `baseURL` as `const baseURL = process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000';` at top of file
   - Set `use.baseURL` to the variable (not hardcoded string)
   - Replace the single `chromium` project with TWO projects:
     - Setup project: `{ name: 'setup', testMatch: /auth\.setup\.ts/ }` -- no device, no storageState, no dependencies
     - Chromium project: `{ name: 'chromium', use: { ...devices['Desktop Chrome'], storageState: '.auth/user.json' }, dependencies: ['setup'] }`
   - Make `webServer` conditional using spread operator: `...(process.env.PLAYWRIGHT_BASE_URL ? {} : { webServer: { command: 'npm run dev', url: 'http://localhost:3000', reuseExistingServer: !process.env.CI, timeout: 120 * 1000 } })`. When `PLAYWRIGHT_BASE_URL` is set (Docker target), webServer is omitted entirely. When not set (local dev), webServer starts `npm run dev`.
   - Keep all other config unchanged: testDir './e2e', fullyParallel true, forbidOnly, retries, workers, reporter 'html', trace 'on-first-retry'

2. Append `.auth/` to `.gitignore` on a new line after the existing `playwright-report/` line. This excludes Playwright storageState files (containing session cookies) from version control.
  </action>
  <verify>
    <automated>grep -c "PLAYWRIGHT_BASE_URL" playwright.config.ts && grep -c "\.auth/" .gitignore && test $(grep -c "PLAYWRIGHT_BASE_URL" playwright.config.ts) -ge 1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains the string `process.env.PLAYWRIGHT_BASE_URL`
    - `playwright.config.ts` contains `name: 'setup'` (the setup project)
    - `playwright.config.ts` contains `dependencies: ['setup']` (chromium depends on setup)
    - `playwright.config.ts` contains `storageState: '.auth/user.json'`
    - `playwright.config.ts` does NOT contain a top-level `webServer:` key when spread logic is used (conditional via spread)
    - `.gitignore` contains `.auth/` on its own line
    - `npx playwright test --list` exits 0 (config is valid, no runtime needed)
  </acceptance_criteria>
  <done>playwright.config.ts reads PLAYWRIGHT_BASE_URL env var, defines setup+chromium projects with storageState dependency, omits webServer when env var is set. .gitignore excludes .auth/ directory.</done>
</task>

</tasks>

<verification>
1. `npx playwright test --list` passes (config is syntactically valid)
2. `grep "PLAYWRIGHT_BASE_URL" playwright.config.ts` finds the env var usage
3. `grep "setup" playwright.config.ts` finds the setup project definition
4. `grep ".auth/" .gitignore` finds the gitignore entry
</verification>

<success_criteria>
- Playwright config is valid and loadable by the test runner
- baseURL is driven by PLAYWRIGHT_BASE_URL with fallback to localhost:3000
- Setup project pattern is defined for auth fixture (consumed by Plan 02)
- .auth/ is gitignored so storageState files are never committed
</success_criteria>

<output>
After completion, create `.planning/phases/12-test-infrastructure/12-01-SUMMARY.md`
</output>
