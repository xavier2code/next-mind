---
phase: 12-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - e2e/fixtures.ts
  - e2e/chat-mock.spec.ts
autonomous: true
requirements:
  - TINF-03

must_haves:
  truths:
    - "Chat E2E tests can mock LLM API responses without real API keys"
    - "route.fulfill intercepts /api/chat requests and returns configurable text"
    - "mockLLMResponse fixture is usable as a test parameter in any E2E test"
  artifacts:
    - path: "e2e/fixtures.ts"
      provides: "Custom test fixture with mockLLMResponse extension"
      contains: "mockLLMResponse"
      min_lines: 15
    - path: "e2e/chat-mock.spec.ts"
      provides: "Smoke test proving LLM mock works end-to-end"
      contains: "mockLLMResponse"
      min_lines: 10
  key_links:
    - from: "e2e/fixtures.ts"
      to: "/api/chat"
      via: "page.route intercept with route.fulfill"
      pattern: "page\\.route.*api/chat"
    - from: "e2e/chat-mock.spec.ts"
      to: "e2e/fixtures.ts"
      via: "import of test with mockLLMResponse"
      pattern: "from.*fixtures"
---

<objective>
Add LLM mock fixture to the E2E test infrastructure so chat-related tests can run without real API keys.

Purpose: E2E tests that exercise chat functionality must not depend on real LLM API calls (D-04, D-06). The mock intercepts at the network level via route.fulfill, requiring zero changes to business code.
Output: Updated e2e/fixtures.ts with mockLLMResponse fixture; smoke test e2e/chat-mock.spec.ts proving the mock works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-infrastructure/12-CONTEXT.md
@.planning/phases/12-test-infrastructure/12-RESEARCH.md
@.planning/phases/12-test-infrastructure/12-02-SUMMARY.md
</context>

<interfaces>
<!-- e2e/fixtures.ts was created by Plan 02 with pass-through test/expect exports -->
<!-- This plan EXTENDS it with mockLLMResponse fixture -->

From e2e/fixtures.ts (Plan 02 output):
```typescript
import { test as base, expect } from '@playwright/test';
export const test = base;
export { expect };
```

Expected after this plan:
```typescript
import { test as base, expect } from '@playwright/test';
export const test = base.extend({
  mockLLMResponse: async ({ page }, use) => {
    await page.route('**/api/chat', async (route) => {
      await route.fulfill({
        status: 200,
        contentType: 'text/plain; charset=utf-8',
        body: 'This is a mock LLM response for E2E testing.',
      });
    });
    await use('This is a mock LLM response for E2E testing.');
  },
});
export { expect };
```

From src/app/api/chat/route.ts:
- Returns `text/plain; charset=utf-8` with chunked transfer encoding
- Client reads response body as text -- mock returns full text atomically (D-05)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add mockLLMResponse fixture and create smoke test</name>
  <files>e2e/fixtures.ts, e2e/chat-mock.spec.ts</files>
  <read_first>
    - e2e/fixtures.ts (current state from Plan 02 -- pass-through exports)
    - src/app/api/chat/route.ts (understand the real endpoint's content type)
  </read_first>
  <action>
Per D-04 (route.fulfill mechanism), D-05 (single complete text response), D-06 (Phase 12 only provides success mock):

**1. Update `e2e/fixtures.ts`:**
- Change from pass-through `base` to extended fixture using `base.extend({...})`
- Add `mockLLMResponse` fixture:
  - Type: `async ({ page }, use) => { ... }`
  - Intercept: `await page.route('**/api/chat', async (route) => { ... })`
  - Fulfill with: `{ status: 200, contentType: 'text/plain; charset=utf-8', body: 'This is a mock LLM response for E2E testing.' }`
  - After route setup, call `await use('This is a mock LLM response for E2E testing.');` so tests can access the mock text
- Keep the `export { expect };` unchanged
- The `**/api/chat` glob pattern matches any host/path containing `/api/chat`, which works for both local and Docker targets

**2. Create `e2e/chat-mock.spec.ts`:**
- Import `{ test, expect }` from `'./fixtures'` (NOT from @playwright/test directly)
- Create a single smoke test in a `test.describe('LLM Mock', ...)` block
- Test named `'mock intercepts chat API and returns response'`:
  - Destructure `{ page, mockLLMResponse }` from test parameters
  - Navigate to `/` (requires auth -- test uses storageState from setup project)
  - The mockLLMResponse fixture is auto-activated because it's a parameter
  - Verify the fixture was set up: `expect(mockLLMResponse).toBe('This is a mock LLM response for E2E testing.');`
  - This is a lightweight smoke test -- it proves the fixture wiring works without triggering actual chat UI interaction (that's Phase 13's job)
  - If the chat page UI changes, this test should not break -- it only verifies the mock fixture is callable
  </action>
  <verify>
    <automated>grep -q "mockLLMResponse" e2e/fixtures.ts && grep -q "api/chat" e2e/fixtures.ts && test -f e2e/chat-mock.spec.ts && grep -q "from.*fixtures" e2e/chat-mock.spec.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/fixtures.ts` contains `base.extend({` (extending base test)
    - `e2e/fixtures.ts` contains `mockLLMResponse` fixture definition
    - `e2e/fixtures.ts` contains `page.route('**/api/chat'` for interception
    - `e2e/fixtures.ts` contains `route.fulfill(` with `contentType: 'text/plain; charset=utf-8'`
    - `e2e/fixtures.ts` exports `test` and `expect`
    - `e2e/chat-mock.spec.ts` imports `{ test, expect }` from `'./fixtures'`
    - `e2e/chat-mock.spec.ts` uses `mockLLMResponse` as a test parameter
  </acceptance_criteria>
  <done>mockLLMResponse fixture intercepts /api/chat via route.fulfill and returns text/plain response. Smoke test proves fixture is importable and callable.</done>
</task>

</tasks>

<verification>
1. `grep "mockLLMResponse" e2e/fixtures.ts` finds the fixture
2. `grep "api/chat" e2e/fixtures.ts` finds the route intercept
3. `grep "route.fulfill" e2e/fixtures.ts` finds the mock response
4. `test -f e2e/chat-mock.spec.ts` passes
5. `npx playwright test --list` still passes
</verification>

<success_criteria>
- mockLLMResponse fixture works as a Playwright test fixture (auto-setup via parameter)
- Route interception pattern matches /api/chat with correct content type
- Smoke test imports from fixtures and references mockLLMResponse
- No real API key needed for chat E2E tests (TINF-03 satisfied)
- Existing E2E tests still list without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-test-infrastructure/12-03-SUMMARY.md`
</output>
