---
phase: 12-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - e2e/auth.setup.ts
  - e2e/fixtures.ts
  - scripts/seed.ts
  - package.json
autonomous: true
requirements:
  - TINF-02
  - TINF-04

must_haves:
  truths:
    - "Auth setup test logs in via credentials provider and saves storageState to .auth/user.json"
    - "Test user (test@nextmind.dev) exists in database after npm run db:seed runs"
    - "db:seed is idempotent -- running twice does not fail or create duplicates"
    - "Custom test fixture re-exports test and expect from Playwright with storageState support"
  artifacts:
    - path: "e2e/auth.setup.ts"
      provides: "Playwright setup project test that performs login and saves session state"
      contains: "storageState"
      min_lines: 15
    - path: "e2e/fixtures.ts"
      provides: "Custom Playwright test fixture with auth state re-export"
      exports: ["test", "expect"]
      min_lines: 5
    - path: "scripts/seed.ts"
      provides: "Idempotent test user seed script using Drizzle ORM"
      contains: "hashPassword"
      min_lines: 20
    - path: "package.json"
      provides: "db:seed npm script entry"
      contains: "db:seed"
  key_links:
    - from: "e2e/auth.setup.ts"
      to: "/login"
      via: "page.goto + fill credentials + submit"
      pattern: "page\\.fill.*email.*test@nextmind\\.dev"
    - from: "scripts/seed.ts"
      to: "src/lib/db"
      via: "relative import of db"
      pattern: "from.*\\.\\./src/lib/db"
    - from: "scripts/seed.ts"
      to: "src/lib/password"
      via: "relative import of hashPassword"
      pattern: "from.*\\.\\./src/lib/password"
    - from: "scripts/seed.ts"
      to: "src/lib/db/schema"
      via: "relative import of users table"
      pattern: "from.*\\.\\./src/lib/db/schema"
---

<objective>
Create the auth fixture infrastructure (setup test + custom fixtures) and the seed script for test user creation.

Purpose: E2E tests need an authenticated session without manual login each time (D-01, D-02). The seed script creates the test user in the database that the auth fixture logs in as (D-03, D-07, D-08).
Output: e2e/auth.setup.ts (setup project test), e2e/fixtures.ts (custom fixtures), scripts/seed.ts (idempotent seed), package.json updated with db:seed script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-infrastructure/12-CONTEXT.md
@.planning/phases/12-test-infrastructure/12-RESEARCH.md
@.planning/phases/12-test-infrastructure/12-01-SUMMARY.md
</context>

<interfaces>
<!-- Executor MUST read these files before implementing -->
<!-- The login form structure determines the selectors used in auth.setup.ts -->

From src/components/auth/login-form.tsx:
- Form has `onSubmit={handleSubmit}` with `button[type="submit"]`
- Email input: `name="email"`, `type="email"`, `required`
- Password input: `name="password"`, `type="password"`, `required`
- Uses `signIn('credentials', { email, password, redirect: false })` then `router.push('/')` on success
- Error display: `text=Invalid email or password` div appears on failure

From src/lib/db/index.ts:
```typescript
export const db = drizzle(queryClient, { schema });
```

From src/lib/password.ts:
```typescript
export async function hashPassword(password: string): Promise<string>;
```

From src/lib/db/schema.ts:
```typescript
export const users = pgTable('user', {
  id: text('id').primaryKey(),
  name: text('name'),
  email: text('email').notNull().unique(),
  password: text('password'),
  // ... other fields
});
```

From playwright.config.ts (after Plan 01):
- Setup project matches `auth.setup.ts`
- Chromium project uses `storageState: '.auth/user.json'` and `dependencies: ['setup']`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create seed script and auth setup test with custom fixtures</name>
  <files>scripts/seed.ts, e2e/auth.setup.ts, e2e/fixtures.ts, package.json</files>
  <read_first>
    - src/lib/db/index.ts (db export pattern)
    - src/lib/db/schema.ts (users table structure -- columns: id, name, email, password, etc.)
    - src/lib/password.ts (hashPassword function signature)
    - src/components/auth/login-form.tsx (form field names and submit button for selectors)
    - package.json (to add db:seed script)
    - playwright.config.ts (setup project pattern from Plan 01)
  </read_first>
  <action>
Per D-01 (storageState reuse), D-02 (seed creates user, auth fixture logs in), D-03 (single test user), D-07 (minimal seed), D-08 (db:seed script):

**1. Create `scripts/seed.ts`:**
- Use RELATIVE imports (NOT @/ alias) because tsx does not resolve path aliases from scripts/:
  - `import { db } from '../src/lib/db/index.js';`
  - `import { users } from '../src/lib/db/schema.js';`
  - `import { hashPassword } from '../src/lib/password.js';`
  - `import { eq } from 'drizzle-orm';`
- Test user credentials: email `test@nextmind.dev`, password `Test123456!`
- Implementation: check if user exists (`db.query.users.findFirst` with `eq(users.email, testEmail)`). If exists, log "Test user already exists, skipping seed." and return. If not, call `hashPassword(testPassword)`, then `db.insert(users).values({ id: crypto.randomUUID(), name: 'Test User', email: testEmail, password: hashedPassword })`. Log "Test user created successfully."
- Wrap in `seed().catch((err) => { console.error('Seed failed:', err); process.exit(1); })` at bottom
- Use a clean `async function seed()` as the main function

**2. Create `e2e/auth.setup.ts`:**
- Import `test as setup` and `expect` from `@playwright/test`
- Define `const authFile = '.auth/user.json';`
- Single test named `'authenticate'` using the `setup` function:
  - `await page.goto('/login');`
  - `await page.fill('input[name="email"]', 'test@nextmind.dev');`
  - `await page.fill('input[name="password"]', 'Test123456!');`
  - `await page.click('button[type="submit"]');`
  - `await page.waitForURL('/', { timeout: 10000 });` -- wait for redirect to home after successful login
  - `await page.context().storageState({ path: authFile });`
  - IMPORTANT: The login form uses `signIn('credentials', { redirect: false })` then `router.push('/')`, so the redirect is client-side. `waitForURL('/')` is the correct wait condition.

**3. Create `e2e/fixtures.ts`:**
- Import `test as base` and `expect` from `@playwright/test`
- Export `const test = base;` (pass-through for now; Plan 03 will extend this with mockLLMResponse)
- Export `{ expect }` from `@playwright/test`
- This file serves as the single import point for all E2E tests: `import { test, expect } from './fixtures';`

**4. Update `package.json` scripts section:**
- Add `"db:seed": "npx tsx scripts/seed.ts"` after the existing `db:studio` line
- Keep all existing scripts unchanged
  </action>
  <verify>
    <automated>grep -q "db:seed" package.json && test -f scripts/seed.ts && test -f e2e/auth.setup.ts && test -f e2e/fixtures.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed.ts` exists and contains `hashPassword` import from relative path `../src/lib/password`
    - `scripts/seed.ts` contains `test@nextmind.dev` as the test user email
    - `scripts/seed.ts` contains idempotent check: queries for existing user before insert
    - `scripts/seed.ts` uses relative imports only (no `@/` aliases)
    - `e2e/auth.setup.ts` exists and contains `storageState({ path:` call with `.auth/user.json`
    - `e2e/auth.setup.ts` fills `input[name="email"]` and `input[name="password"]`
    - `e2e/auth.setup.ts` calls `page.waitForURL('/')` after submit
    - `e2e/fixtures.ts` exists and exports both `test` and `expect`
    - `package.json` contains `"db:seed": "npx tsx scripts/seed.ts"` in scripts section
  </acceptance_criteria>
  <done>Seed script creates test user idempotently. Auth setup test logs in and saves session. Fixtures file provides unified test/expect imports. db:seed npm script registered.</done>
</task>

</tasks>

<verification>
1. `grep -c "db:seed" package.json` returns 1 or more
2. `test -f scripts/seed.ts` passes
3. `test -f e2e/auth.setup.ts` passes
4. `test -f e2e/fixtures.ts` passes
5. `grep "test@nextmind.dev" scripts/seed.ts` finds the test user email
6. `grep "storageState" e2e/auth.setup.ts` finds the state save
7. `grep "from.*@playwright/test" e2e/fixtures.ts` finds the import
8. `npx playwright test --list` still passes (config + new files are valid)
</verification>

<success_criteria>
- All 4 files exist with correct content
- Seed script is idempotent (checks before insert)
- Auth setup test uses the correct form selectors matching login-form.tsx
- Fixtures file exports test and expect for downstream consumption
- package.json has db:seed script matching existing db:* naming convention
- Playwright config still validates with new setup test file
</success_criteria>

<output>
After completion, create `.planning/phases/12-test-infrastructure/12-02-SUMMARY.md`
</output>
