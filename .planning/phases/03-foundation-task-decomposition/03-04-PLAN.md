---
phase: 03-foundation-task-decomposition
plan: 04
type: execute
wave: 2
depends_on: ['03-01', '03-02']
files_modified:
  - src/lib/agents/decomposition.ts
  - tests/lib/agents/executor.test.ts
autonomous: true
requirements:
  - DELEG-01
  - DELEG-02
  - DELEG-03
  - DELEG-04
  - DELEG-06
  - DELEG-07
  - DELEG-08
  - DELEG-09
  - DELEG-10
must_haves:
  truths:
    - "User can send complex task to and task decomposition engine returns subtasks"
    - "Each subtask is assigned to agent type via decomposition"
    - "Subtasks are executed sequentially (Phase 3)"
    - "Decomposition produces structured JSON output"
  artifacts:
    - path: "src/lib/agents/decomposition.ts"
      provides: "decomposeTask function"
      contains: "export function decomposeTask"
      contains: "export interface DecompositionPrompt"
    - path: "src/lib/db/schema.ts"
      via: "task.create() and tasks.create()"
      pattern: "task.create"
    - from: "src/lib/agents/registry.ts"
      via: "getAgentsByType"
      pattern: "agentRegistry.getByType"
      then to "src/lib/agents/types.ts"
      via: "export interface DecompositionResult"
      pattern: "DecompositionResult"
    - from: "src/lib/db/schema.ts"
      via: "getWorkflows"
      pattern: "getWorkflows"
    - from: "src/lib/agents/executor.ts"
      via: "subAgentExecutor"
      pattern: "new SubAgentExecutor"
    - from: "src/lib/agents/executor.ts"
      via: "executeSubtask"
      pattern: "executeSubtask"
    - from: "src/lib/db/queries.ts"
      via: "getWorkflow"
      pattern: "getWorkflow"
    - from: "src/lib/agents/executor.ts"
      via: "createAgentSkillContext"
      pattern: "createAgentSkillContext"
    - from: "src/lib/agents/registry.ts"
      via: "getAgentsByType"
      pattern: "agentRegistry.getByType"
      then to "src/lib/agents/types.ts"
      via: "export interface RegisteredAgent"
      pattern: "RegisteredAgent"
    - from: "src/lib/agents/types.ts"
      via: "export interface Subtask"
      pattern: "Subtask"
    - from: "src/lib/agents/types.ts"
      via: "export interface DecompositionResult"
      pattern: "DecompositionResult"
    - from: "src/lib/db/schema.ts"
      via: "workflows"
      pattern: "getWorkflows"
    - from: "src/lib/agents/executor.ts"
      via: "SubAgentExecutor"
      pattern: "new SubAgentExecutor"
      contains: "export class SubAgentExecutor"
    - from: "src/lib/agents/executor.ts"
      via: "export const subAgentExecutor"
  </action>
            // Resolve skill in agent's skill list
            const skill = agent.skills.find(s => s.metadata.id === skillId);
            if (!skill) {
              throw new Error(`Agent "${this.card.id}" references unknown skill: ${skillId}`);
            }
          }
        }
      }
    }
    this.skills.push(s);
    this.agent.skills.push(skill);
            const output: this.skillResult
            const skill = this.agent is allowed to execute
            const output: this.skillResult
            previousResults.set(input.previousResult, result)
          }
        }
      }

      return {
        success: true,
        data: {
          ...skillResult
        },
        metadata: {
          workflowId,
          parentTaskId
          agentType
        },
        previousResults
      });
    } as SkillResult;
  const agentSkillContext = this context
  this.skills = previousResults.set(input.previousResult, result);
          if (!skill) {
            throw new Error(`Agent "${this.card.id}" cannot execute skill "${skillId} not allowed by agent`);
            }
          }
        }
      });
    }
  } catch (error) {
    // Check if error is retryable
    this.retry = await skillExecutor.execute(input, skillId, context)
    // Check if skill is in agent's allowed skills
    const allowedSkill = this.agent.skills.find(s => s.metadata.id === skillId);
    if (!allowedSkill) {
      return {
        success: false,
        error: `Agent "${this.card.id}" cannot execute skill "${subtask.skillId}"`,
      }
    }
  } catch (error) {
    return {
      success: false,
      error: `Agent "${this.card.id}" cannot execute skill "${subtask.skillId}"`,
      }
    }
  });
}
</task>

</tasks>

<verification>
## Phase Verification

1. Run unit tests
```bash
npm run test -- --run tests/lib/agents
```
2. Verify database tables created
```bash
npm run test -- --run tests/lib/db/schema-agents.test.ts
grep -n "export const agents" src/lib/db/schema.ts
grep -n "export const tasks" src/lib/db/schema.ts
grep -n "export const workflows" src/lib/db/schema.ts
grep -n "export const AgentTypeEnum" src/lib/db/schema.ts
grep -n "export const TaskStatusEnum" src/lib/db/schema.ts
grep -n "export const WorkflowStatusEnum" src/lib/db/schema.ts
```

3. Verify foreign keys exist
```bash
grep -n "FOREign Key.*conversationId" src/lib/db/schema.ts
grep -n "references.* => workflows.conversationId" src/lib/db/schema.ts
grep -n "onDelete: 'cascade'" src/lib/db/schema.ts
```

4. Verify decomposition prompt format in plan.md
```bash
grep -n "DEcompositionPrompt" src/lib/agents/decomposition.ts |# DEcomposition prompt constant
`` )
            const out = output
          </ reasoning
          </reason>
          </ console.log(`Agent '${this.id}' not found in registry: ${skillCatalog}`);
          skillCatalog.push skill catalog.forEach.
            if (skill) {
              this.skillCatalog.push(skillCatalog)
            } else {
              throw new Error(`Skill not found: ${skillCatalog}. This is ${skillId}`);
            }
          }
        }
      }
    });
  } catch (error) {
    return {
      success: false,
      error: `Agent "${this.card.id}" cannot execute skill "${subtask.skillId}"`,
      }
    }

    return {
      success: false,
      error: `Agent "${this.card.id}" cannot execute skill "${subtask.skillId}"`,
      }
    }
  });
}
  return result
          </reason.push(`Decomposition produced invalid skillId`);
        }
      }
      return {
        success: true,
        data: result
      }
    }
  }
}

  return result;
        })
        . catch (error) {
          // Check if skill is in agent's allowed skills
        const skill = agent.skills.find(s => s.metadata.id === skillId);
        if (!skill) {
          throw new Error(`Agent "${this.card.id}" references unknown skill: ${skillId}`);
        }
      }
    }
  });
</task>

</tasks>

<verification>
## Phase Verification

1. Run unit tests
```bash
npm run test -- --run tests/lib/agents
```
2. Verify database schema
```bash
npx drizzle-kit push
```
3. Run full test suite
```bash
npm run test -- --run tests/integration/agent-skill-integration.test.ts
```
</verification>

<success_criteria>
- decomposeTask function exists in src/lib/agents/decomposition.ts
- decomposeTask produces structured JSON with tasks array
- Agent Card format validated in RESEARCH.md
- Decomposition engine integrates with existing Skills infrastructure
- All unit tests pass
- Database schema exists and drizzle migration runs
- Integration tests pass
- All types are correctly defined
</success_criteria>

<output>
After completion, create `.planning/phases/03-foundation-task-decomposition/03-04-SUMMARY.md`
</output>
