---
phase: 10
plan: 04
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User can upload a file, see it in the chat input, send a message with the file, and the AI responds based on the file content"
    - "User can edit file content in the FileChip inline editor, send the message, and the AI uses the edited version"
    - "Attachment bar renders below user messages showing filename, icon, and size"
    - "File skills appear in the skill registry and can be invoked by agents"
  artifacts: []
  key_links: []
---

<objective>
Manual verification of the complete Phase 10 chat and skills integration. The executor verifies that file content injection works end-to-end, the inline editor functions correctly, the attachment bar displays properly, and all file processing skills are registered and accessible.

Purpose: Confirm the phase delivers the expected user-facing behavior before marking it complete.

Output: Verified (or issue report) from human testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-chat-skills-integration/10-CONTEXT.md
@.planning/phases/10-chat-skills-integration/10-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify chat and skills integration end-to-end</name>
  <files></files>
  <read_first>
    - .planning/phases/10-chat-skills-integration/10-01-SUMMARY.md (injection module summary)
    - .planning/phases/10-chat-skills-integration/10-02-SUMMARY.md (skills summary)
    - .planning/phases/10-chat-skills-integration/10-03-SUMMARY.md (UI integration summary)
  </read_first>
  <action>
This is a human verification checkpoint. No code changes. The executor presents the verification steps to the user and waits for approval.

Plans 10-01 through 10-03 implemented:
- File content injection module (src/lib/chat/inject-file-content.ts)
- 5 file processing skills (src/skills/file-processing.ts)
- FileChip inline editor (src/components/files/file-chip.tsx)
- Attachment bar on user messages (src/components/chat/chat-message.tsx)
- handleSend wired to injectFileContent in both chat pages
- useFileUpload extended with editedContent tracking

Verification steps for the user:
1. Start dev server: `npm run dev`
2. Test file upload and injection (CHAT-01/02/03): Upload file, wait for extraction, send message with file, verify AI responds based on file content, verify attachment bar appears
3. Test inline editing (CHAT-05): Click edit on FileChip, modify content, save, send message, verify AI uses edited version
4. Test truncation (CHAT-04): Upload large file(s), verify truncation warning appears
5. Test skills registration (SKIL-01/02/03/04): Check skills sidebar, verify all 5 file skills listed
6. Test AI message no attachment (D-09): Send message with file, verify AI response has no attachment bar
  </action>
  <verify>
    <automated>echo "Manual verification checkpoint -- no automated verify command"</automated>
  </verify>
  <done>User confirms all verification steps pass, or reports specific issues to address</done>
  <what-built>
    Plans 10-01 through 10-03 implement:
    - File content injection module (src/lib/chat/inject-file-content.ts)
    - 5 file processing skills (src/skills/file-processing.ts)
    - FileChip inline editor (src/components/files/file-chip.tsx)
    - Attachment bar on user messages (src/components/chat/chat-message.tsx)
    - handleSend wired to injectFileContent in both chat pages
    - useFileUpload extended with editedContent tracking
  </what-built>
  <how-to-verify>
    1. **Start dev server**: `npm run dev`
    2. **Test file upload and injection (CHAT-01/02/03)**:
       - Navigate to chat page
       - Upload a PDF or text file via drag-drop or attachment button
       - Wait for extraction to complete (status shows "ready" on FileChip)
       - Type a question about the file content (e.g., "Summarize this file")
       - Send the message
       - Verify: AI responds based on the file content (not just the text prompt)
       - Verify: Attachment bar appears below the user message showing filename, icon, size
       - Click the attachment item and verify it navigates to `/files?id={fileId}`
    3. **Test inline editing (CHAT-05)**:
       - Upload another file and wait for extraction
       - Click the pencil (edit) button on the FileChip
       - Verify: Inline textarea expands showing extracted Markdown content
       - Edit the content (e.g., change some text)
       - Click "保存" (Save)
       - Verify: Pencil icon turns blue indicating edits saved
       - Send a message referencing the file
       - Verify: AI responds based on the edited version, not the original
    4. **Test truncation (CHAT-04)**:
       - Upload a large file or attach multiple files totaling >10000 characters
       - Send a message
       - Verify: Truncation warning appears ("文件内容过长，已截断显示前 N 字符")
    5. **Test skills registration (SKIL-01/02/03/04)**:
       - Navigate to the skills sidebar panel
       - Verify: file-extract, file-convert, file-classify skills appear
       - Verify: file-read and file-list are present (updated versions)
       - Check console/network for `/api/skills` endpoint and verify all 5 skills are listed
    6. **Test AI message no attachment (D-09)**:
       - Send a message with a file
       - Verify: The AI response does NOT show an attachment bar
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
- Human confirms file content injection works end-to-end
- Human confirms inline editing works and AI uses edited content
- Human confirms attachment bar renders correctly
- Human confirms skills are registered and accessible
- Human confirms no regressions in existing chat functionality
</verification>

<success_criteria>
- All CHAT-01 through CHAT-05 requirements verified by human testing
- All SKIL-01 through SKIL-04 skills registered and discoverable
- No regressions in existing chat, file upload, or extraction functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-chat-skills-integration/10-04-SUMMARY.md`
</output>
